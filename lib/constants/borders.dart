import 'dart:ui';

import 'package:flutter/cupertino.dart';

class AppBorders {
  static BorderRadius small = BorderRadius.circular(5);
  static BorderRadius medium = BorderRadius.circular(10);
  static BorderRadius large = BorderRadius.circular(15);
  static BorderRadius extraLarge = BorderRadius.circular(20);
}
